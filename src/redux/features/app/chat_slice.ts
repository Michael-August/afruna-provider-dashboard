import { createSlice } from "@reduxjs/toolkit";

const initialState = {
    conversations: [],
    chatDetails: {}
}

const chat_slice = createSlice({
    name: "Chat_Slice",
    initialState,
    reducers: {
        
    }
})